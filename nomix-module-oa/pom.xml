<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.lxzy.nomix</groupId>
        <artifactId>nomix</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>nomix-module-oa</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        oa 包下，办公自动化（Office Automation），提供协同办公、行政管理和业务审批功能。
        例如说：日程计划、企业云盘、请假报销、会议室、车辆、印章、公文等等
    </description>

    <dependencies>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-system</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-infra</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-bpm</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- Job 定时任务相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-job</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
        </dependency>
        <dependency>
            <groupId>com.belerweb</groupId>
            <artifactId>pinyin4j</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
