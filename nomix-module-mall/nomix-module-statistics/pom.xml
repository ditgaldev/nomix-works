<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.lxzy.nomix</groupId>
        <artifactId>nomix-module-mall</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>nomix-module-statistics</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        statistics 模块，主要实现统计相关功能
        例如：统计商品、会员、交易等功能。
    </description>

    <dependencies>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-promotion</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-product</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-trade-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-member</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-module-pay</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-biz-tenant</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-biz-ip</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-test</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>com.lxzy.nomix</groupId>
            <artifactId>nomix-spring-boot-starter-excel</artifactId>
        </dependency>
    </dependencies>

</project>
